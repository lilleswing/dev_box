---
  - name: ensure apt cache is up to date
    sudo: true
    apt: update_cache=yes
  - name: ensure packages are installed
    apt: name={{item}}
    sudo: true
    with_items:
        - postgresql
        - postgresql-contrib
        - postgresql-client
        - libpq-dev
        - python-psycopg2
        - pgadmin3
