---
- name: Apt-Get Update
  become: true
  apt: update_cache=yes
  when: ansible_os_family == "Debian"

- name: Install VLC
  become: true
  apt: name={{item}} force=yes state=present
  with_items:
    - vlc
    - browser-plugin-vlc
    - libavcodec-extra
  when: ansible_os_family == "Debian"
